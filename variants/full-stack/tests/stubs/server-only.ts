// Empty stub for the `server-only` package so server modules can be imported in
// the Vitest (node) environment. In the app itself the real package still guards
// these modules against being bundled into a client component.
export {};
